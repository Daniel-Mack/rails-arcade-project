<h1>Game: <%= @game.name %></h1>

<h3>Select a character to start the game</h3>

<% @game.characters.each do |character| %>
  <p><%= link_to "#{character.name}", character_path(character.game) %></p>
<% end %><br>
